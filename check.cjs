const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5174/external-registration', { waitUntil: 'networkidle0' }); const html = await page.content(); const fs = require('fs'); fs.writeFileSync('dom.html', html); await page.screenshot({ path: 'screenshot.png' }); await browser.close(); })();
